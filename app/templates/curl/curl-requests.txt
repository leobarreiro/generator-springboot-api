# Hello
curl -X GET http://localhost:<%=portNumber%>/<%=artifact%>/hello

# Date-now
curl -X GET http://localhost:<%=portNumber%>/<%=artifact%>/date/now
<% if (rabbit) { %>
# Rabbit
curl -X POST -H "Content-type: application/json" -d "{\"name\": \"<%=randomName%>\", \"surname\": \"<%=randomSurname%>\"}" http://localhost:<%=portNumber%>/<%=artifact%>/rabbit/send
<% } if (kafka) { %>
# Kafka
curl -X POST -H "Content-type: application/json" -d "{\"name\": \"<%=randomName%>\", \"surname\": \"<%=randomSurname%>\"}" http://localhost:<%=portNumber%>/<%=artifact%>/kafka/send
<% } if (jpa) {%>
# Database Save
curl -X POST http://localhost:<%=portNumber%>/<%=artifact%>/registry/save -d "description=Saving a new one"

# Database Update
curl -X PUT http://localhost:<%=portNumber%>/<%=artifact%>/registry/update/1 -d "description=Updating a registry"

# Database List all
curl -X GET http://localhost:<%=portNumber%>/<%=artifact%>/registry/list-all

# Database Delete
curl -X DELETE http://localhost:<%=portNumber%>/<%=artifact%>/registry/remove/1<% } %>

